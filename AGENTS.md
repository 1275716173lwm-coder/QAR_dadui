1.源文件解释：
    1.这个系统是用于分析三种飞机发动机型号（CFM、LEAP、PW）的飞机起降数据

    2.这里面有几个子文件夹，其结构如下:
        --data
            --folders_names
             --name1
                    --docs
             --name2
                    --docs
             etc
    
     --result_samples
           --result_sample.xlsx
    
        --AGENTS.md

     --OTHER DOCS

    3.文件解释：
        3.1.对于data文件下的指定子文件夹名字代表了不同人员的数据文件，比如name1就是一个人员的名字，在name1文件夹中代表了name1人员的所有飞机操作数据，以此类推到data下文件夹中的所有人员，如果某个子文件夹中没有数据，则代表这次分析中该人员无需分析该种机型的数据


        3.2对于data文件夹中的人员文件中的数据子文件夹中，csv数据文件的命名规则是文件名包括YZDCFM的是对于CFM飞机，包含YZDLEAP的是LEAP飞机，包含YZDPW的是PW飞机。注意在之后识别时要包含且仅包含上述命名规则的时候再识别其飞机类型


2.你的工作有如下两点：

    2.1 表格形式分析
            弹出一个可人工选择数据文件夹，选择后再继续

            以 result_sample 中的 result_sample.xlsx 作为只读模板。程序在运行目录下创建 result 文件夹（若已存在则直接使用），每次开始分析时都重新从模板创建一份空白工作簿用于本次分析，结果表输出为 result/qar.xlsx。已有的 result/qar.xlsx 只能视为上一次运行的结果，不能作为下一次运行的模板或数据源。写入时先在 result 文件夹生成临时文件，全部分析和保存成功后再替换 result/qar.xlsx，避免上一次的人员数据、公式或格式残留，也避免程序中途退出后留下不完整结果。不再寻找或调用 blank.xls。

            同上，在 name1 这些姓名文件夹中会有除上述 cfm、leap、pw 文件夹之外还有命名规则类似于“B-6382_20260519040339___S1_E1_YZDCFM_CA2641.csv”的文件，每个人会有若干个这种文件，YZDCFM 代表该人 CFM 飞机的数据，20260519040339 只判断前半部分读取其日期为2026.5.19，航段为 CA2641。同理推广至 LEAP 和 PW 飞机的数据。需要做的就是将这些数据以及下面描述的特征数据按照人员和机型的不同录入到 result/qar.xlsx，表格中没有的不需要进行录入。

            对选中的数据文件夹中的每个子文件夹进行遍历，比如：选择的是 data 文件夹，就需要遍历所有人员的文件。实际数据目录以程序运行时人工选择的目录为准，不额外搜索该目录之外的数据。人员文件夹按文件夹名称升序处理；同一人员的文件先从文件名中解析 YYYYMMDDhhmmss 时间并按时间升序处理，解析时间相同或无法解析时间时再按完整文件名升序处理。分析完该人的所有文件后，再继续处理下一人。不得依赖操作系统返回的默认文件排列顺序。

            表格中：
            按列说明
                A. 姓名
                    填充选择的数据文件夹中当场分析时的文件名，比如选择的 data 文件，在分析到 name1 文件下的若干文件时，表格中姓名列就填充为name1

                B.低空 300ft AGL 以下杆量超过 15 个单位采样点数
                    按顺序打开数据文件，判断其为 CFM、LEAP 还是 PW 飞机，对于单个文件按照下述规则进行分析
                    对于 CFM 机型：
                        参考 D 列，找到数据第一次从 2000 以上下降至第一次最接近 300 时的行数并记录为 B11 行（此时也就是飞机 300ftAGL 时刻数据行），再找到数据第一次从2000以上下降至第一次变为负值时的行数并记录为TDCFM行（此时也就是飞机接地时刻数据行），在B11-AM 单元格与 TDCFM-BB 单元格围成的矩形方格中寻找有效数值中绝对值大于15的采样点个数并暂存为数组Beff中一个数值，有效数值采样点总数暂存为数组Ball中一个数值

                    对于 LEAP 机型：
                        参考 D 列，找到数据第一次从 2000 以上下降至第一次最接近 300 时的行数并记录为 B21 行（此时也就是飞机 300ftAGL 时刻数据行），再找到数据第一次从 2000 以上下降至第一次变为负值时的行数并记录为 TDLEAP 行（此时也就是飞机接地时刻数据行），在 B21-AE 单元格与 TDLEAP-AT 单元格围成的矩形方格中寻找有效数值中绝对值大于15的采样点个数并暂存为数组Beff中一个数值，有效数值采样点总数暂存为数组Ball中一个数值

                    对于 PW 机型：
                        参考 D 列，找到数据第一次从 2000 以上下降至第一次最接近 300 时的行数并记录为 B31 行（此时也就是飞机 300ftAGL 时刻数据行），再找到数据第一次 2000 以上下降至第一次变为负值时的行数并记录为 TDPW 行（此时也就是飞机接地时刻数据行），在B31-AE 单元格与 TDPW-AT 单元格围成的矩形方格中寻找有效数值中绝对值大于15的采样点个数并暂存为数组Beff中一个数值，有效数值采样点总数暂存为数组Ball中一个数值

                    统计区域内只有能够成功转换为有限数值的单元格才是有效采样点。Beff 记录有效采样点中绝对值严格大于15的数量；Ball 记录全部有效采样点数量。空白、文本、错误值、正负无穷值和无法转换为数值的内容均不计入 Beff 或 Ball。

                    当遍历完一个人的所有数据文件时，计算 Beff 数组中所有值的和，作为该人的超限采样点总数填充到B列
                        
                C.低空 300ft AGL 以下杆量超过 15 个单位采样点占比
                    计算上述 Beff 数组中所有数据的和与 Ball 数组中所有数据的和之比，以百分比形式填充。Ball 总和为0时该单元格留空，不填写0%，并在日志中记录该人员没有可用于此指标的有效采样点。

                D.低空 50ft AGL 以下舵量超过 15 个单位次数（暂不填充）
                    当前版本不计算、不写入任何数值或占位文本，D列数据单元格保持空白；后续更新分析规则后再录入。
                    

                E.低空 50ft AGL 以下舵量超过 15 个单位占比（暂不填充）
                    当前版本不计算、不写入任何数值或占位文本，E列数据单元格保持空白；后续更新分析规则后再录入。


                F.接地交叉角大于 6° 次数
                    按顺序打开数据文件，判断其为CFM、LEAP还是PW飞机，对于单个文件按照下述规则进行分析
                        对于 CFM 机型：
                            参考上述得到的该次接地时刻数据行 TDCFM 行，BG列-TDCFM 行的数值是接地时飞机磁航向，记录为 MHAC；BH列-TDCFM 行的数值是着陆跑道磁航向（若无有效值，则从该单元格沿该列向上寻找第一个有效数值），记录为 MHRWY。航向差必须按圆周角计算：ANGLE_DIFF = abs(((MHAC - MHRWY + 180) mod 360) - 180)。若 ANGLE_DIFF 严格大于6°，则记录当次接地交叉角大于6°。

                        对于 LEAP 机型：
                            参考上述得到的该次接地时刻数据行 TDLEAP 行，AW列-TDLEAP 行的数值是接地时飞机磁航向，记录为 MHAC；AX列-TDLEAP 行的数值是着陆跑道磁航向（若无有效值，则从该单元格沿该列向上寻找第一个有效数值），记录为 MHRWY。按照上述 ANGLE_DIFF 圆周角公式计算航向差，若严格大于6°，则记录当次接地交叉角大于6°。

                        对于 PW 机型：
                            参考上述得到的该次接地时刻数据行 TDPW 行，AY列-TDPW 行的数值是接地时飞机磁航向，记录为 MHAC；AZ列-TDPW 行的数值是着陆跑道磁航向（若无有效值，则从该单元格沿该列向上寻找第一个有效数值），记录为 MHRWY。按照上述 ANGLE_DIFF 圆周角公式计算航向差，若严格大于6°，则记录当次接地交叉角大于6°。

                    当遍历完一个人的所有数据文件时，计算上述过程中接地交叉角大于6°的总次数并录入单元格

                G.接地交叉角大于 6° 占比

                    对该指标分别记录：符合文件名规则的候选文件数、成功完成交叉角计算的有效文件数、交叉角大于6°的触发文件数。占比使用“触发文件数 ÷ 有效文件数”，以百分比表示。因缺列、无法定位接地行、无法取得有效航向等原因计算失败的文件不得计入有效文件数，也不得按“未触发”处理；应写入日志。有效文件数为0时该单元格留空，不填写0%。




                H.PF 抬头速率大于 3.5°/s 次数
                
                    对于 CFM 机型：
                        打开 CFM 飞机的数据表格，自上而下扫描数据行，参考 D 列至 J 列以及 K 列至 Q 列：
                            当同一行中，D-J 列至少有 2 个值大于2，且 K-Q 列至少有 2 个值大于2时，将首次满足该条件的行记录为起飞离地时刻所在行，为 TOCFM 行。
                            这里记录的是满足条件时所在的行号，不需要区间行数

                        TOCFM-1 行至 TOCFM+2 行 与 BL 列至 BO 列区域中的数据为抬头速率，检测该区域所有数据，若大于3.5，则记录该次起飞抬头速率大于 3.5°/s 

                    对于 LEAP 飞机：
                        打开 LEAP 飞机的数据表格，自上而下扫描数据行，参考 D 列至 H 列以及 I 列至 M 列：
                            当同一行中，D-H 列至少有 1 个值大于2，且 I-M 列至少有 1 个值大于2时，将首次满足该条件的行记录为起飞离地时刻所在行，为 TOLEAP 行。
                            这里记录的是满足条件时所在的行号，不需要区间行数

                        TOLEAP-1 行至 TOLEAP+2 行 与 BB 列至 BE 列区域中的数据为抬头速率，检测该区域所有数据，若大于3.5，则记录该次起飞抬头速率大于 3.5°/s 

                    对于 PW 飞机：
                        打开 PW 飞机的数据表格，自上而下扫描数据行，参考 D 列至 H 列以及 I 列至 M 列：
                            当同一行中，D-H 列至少有 1 个值大于2，且 I-M 列至少有 2 个值大于2，将首次满足该条件的行记录为起飞离地时刻所在行，为 TOPW 行。
                            这里记录的是满足条件时所在的行号，不需要区间行数

                        TOPW-1 行至 TOPW+2 行 与 BD 列至 BG 列区域中的数据为抬头速率，检测该区域所有数据，若大于3.5，则记录该次起飞抬头速率大于 3.5°/s 
                        
                    当遍历完一个人的所有数据文件时，计算上述过程中抬头速率大于 3.5°/s的总次数并录入单元格

                I.PF 抬头速率大于 3.5°/s 占比
                    对该指标分别记录：符合文件名规则的候选文件数、成功完成抬头速率判断的有效文件数、抬头速率大于3.5°/s的触发文件数。占比使用“触发文件数 ÷ 有效文件数”，以百分比表示。因缺列、无法定位离地行或抬头速率区域无有效数值而失败的文件不得计入有效文件数，也不得按“未触发”处理；应写入日志。有效文件数为0时该单元格留空，不填写0%。


                J.抬头速率大中高高原次数
                    在上述H项检查过程中，如果判定该次抬头速率大于 3.5°/s，则需核实该次是否为高高原起飞，方法为：
                        对于 CFM 飞机：
                            检查 TOCFM行-BK列 数据是否大于8000，若是，则该次为高高原起飞
                        对于 LEAP 飞机：
                            检查 TOLEAP行-BA列 数据是否大于8000，若是，则该次为高高原起飞
                        对于 PW 飞机：
                            检查 TOPW行-BC列 数据是否大于8000，若是，则该次为高高原起飞

                    当遍历完一个人的所有数据文件时，计算上述过程中抬头速率大于 3.5°/s时且为高高原起飞的总次数并录入单元格


                K.抬头速率大中高高原占比
                    该指标只以“抬头速率大于3.5°/s，并且成功读取对应离地时刻气压高度”的文件作为有效文件。气压高度大于8000ft的有效文件计入J列高高原次数；无法读取或无法将气压高度转换为有效数值的文件不计入高高原次数，也不计入本指标的有效文件数，并写入日志。

                    当遍历完一个人的所有数据文件时，K列使用“J列高高原次数 ÷ 成功完成高高原判断且抬头速率大于3.5°/s的有效文件数”计算，以百分比形式录入。有效文件数为0时该单元格留空，不填写0%。

            每个文件的每个指标必须独立计算，并分别返回“计算成功或失败、指标值或触发状态、错误原因”。某一个指标失败时，只跳过该文件的该项指标，不得导致该文件其他可计算指标被丢弃，也不得中断该人员或后续人员的处理。

            完成一个人的全部文件后，再按各指标自己的有效文件范围进行人员级汇总并写入该人员所在行。汇总完成后清空该人员的全部临时数组、计数器和错误状态，禁止沿用到下一人员。人员级结果写入成功后再处理下一人员；全部人员处理完成后统一保存工作簿和日志。结果表和日志都输出到程序运行目录下的 result 文件夹。





    2.2 文档形式分析（暂不生成）
        侧风分量（左负右正）（暂不录入）
            地面风向风速：
                对于 CFM 发动机：在接地时刻 TDCFM行-BI列 的数值是风速、TDCFM行-BJ列 是风向
                对于 LEAP 发动机：在接地时刻 TDLEAP行-AY列 的数值是风速、TDLEAP行-AZ列 是风向
                对于 PW 发动机：在接地时刻 TDPW行-BA列 的数值是风速、TDPW行-BB列 是风向
            侧风计算中，读取到的风向和着陆跑道航向均统一视为磁航向，不进行真航向与磁航向之间的磁差换算。将读取到的风向归一化到 [0, 360) 范围：若风向为负值，则持续加360；若风向大于等于360，则对360取模。归一化后的风向记录为 MWD。
            参考上述着陆跑道磁航向 MHRWY，使用角度制计算 CROSSWIND = WIND_SPEED * sin((MWD - MHRWY) * pi / 180)。程序必须用已知风向案例验证符号方向；最终统一保证左侧风为负值、右侧风为正值。若风速、风向或跑道航向无法取得有效数值，则该文件的侧风分量计算失败、结果留空并写入日志。该项目当前暂不写入 result/qar.xlsx，仅保留为后续文档分析指标。




注意：
    如果某个文件或多个文件进行检查分析时找不到相应数据或发生其他错误，按“单文件、单指标”粒度记录失败并继续后续分析，不得使整个程序停止。
    
    日志使用UTF-8编码的TXT文本格式，每条记录占一行，各字段之间使用制表符（Tab，\t）分隔。日志命名为 qar_log_YYYYMMDD_HHmmss.txt，其中时间为本次程序开始运行的本地日期时间；日志文件不得覆盖已有文件，如果同一秒内文件名已存在，则依次追加 _1、_2 等序号。日志与 qar.xlsx 一并输出到程序运行目录下的 result 文件夹。

    日志至少记录：本次运行开始和结束时间、所选数据目录、人员、文件完整路径、识别机型、指标名称、处理状态、错误类型和错误说明。日志还应为每个人、每个指标汇总候选文件数、有效文件数、触发文件数和失败文件数，使 result/qar.xlsx 中的采样点数、次数与占比可以追溯核对。即使本次分析没有任何错误，也必须生成日志并明确记录“未发现错误”。
